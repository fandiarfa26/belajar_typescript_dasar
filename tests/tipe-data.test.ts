describe("Data type", () => {
  it("should must declare", () => {
    const name: string = "M. Fandi Arfabuma";
    const money: number = 10000;
    const isVip: boolean = false;
    console.info(name);
    console.info(money);
    console.info(isVip);
  });
});
