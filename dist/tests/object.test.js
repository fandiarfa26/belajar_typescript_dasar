"use strict";
describe("Object", () => {
    it("should support in typescript", () => {
        const person = {
            id: "1",
            name: "Fandi",
        };
        console.info(person);
    });
});
