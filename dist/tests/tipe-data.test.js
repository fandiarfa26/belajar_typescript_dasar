"use strict";
describe("Data type", () => {
    it("should must declare", () => {
        const name = "M. Fandi Arfabuma";
        const money = 10000;
        const isVip = false;
        console.info(name);
        console.info(money);
        console.info(isVip);
    });
});
